node_modules/
dist/
.env
*.log
.DS_Store

# Local-only onboarding PDF tooling — keep on disk, never commit
docs/
scripts/generate-onboarding-pdf.js

# Mockup / sample proposal generators — local only, may reference real customer data
mockups/

# Tesseract OCR language data — large binary, fetched on-demand when OCR_ENABLED=true
server/eng.traineddata

# QR-code generator output — regenerable from server/scripts/generate-qr-files.js
qr-output/

# Apply-script log + CSV outputs — local-only artifacts
server/scripts/out/

# Regenerable puppeteer / test screenshot output — never commit
server/scripts/test-output/
server/scripts/e2e-phase3-out/
server/scripts/_*-out/
server/scripts/fix9-out/
server/scripts/tmp-*.mjs
